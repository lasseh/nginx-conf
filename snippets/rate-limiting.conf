# =============================================================================
# RATE LIMITING SNIPPET (DISABLED BY DEFAULT)
# =============================================================================
# This snippet provides rate limiting for location blocks
# See docs/RATE-LIMITING.md for comprehensive configuration guide
#
# To use this snippet:
# 1. Enable rate limit zones in nginx.conf (uncomment limit_req_zone lines)
# 2. Include this snippet or add limit_req directives to location blocks
#
# Example usage in location blocks:
#   include snippets/rate-limiting.conf;
#
# Or apply directly:
#   limit_req zone=general burst=10 nodelay;
#   limit_req zone=api burst=20 nodelay;

# General rate limiting (disabled by default)
# Uncomment to enable:
# limit_req zone=general burst=5 nodelay;

# Additional protection for login endpoints (disabled by default)
# location ~ ^/(login|wp-login\.php|admin) {
#     limit_req zone=api burst=3 nodelay;
#
#     # Additional security for admin areas
#     # Uncomment and configure as needed:
#     # allow 192.168.1.0/24;
#     # deny all;
# }
